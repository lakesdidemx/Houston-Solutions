import { Link } from 'react-router-dom';
import { CheckCircle2, ChevronDown, MapPin, ShieldCheck, Wrench, Layers3 } from 'lucide-react';
import { siteConfig, Service } from '../data/site';
export function CTA({title='Ready to improve your property?',text='Tell us what you need and request a free estimate today.'}:{title?:string;text?:string}){return <section className="cta"><div className="container ctarow"><div><h2>{title}</h2><p>{text}</p></div><div className="actions"><Link className="btn primary" to="/request-estimate">Get a Free Estimate</Link><a className="btn light" href={siteConfig.phoneHref}>Call Now</a></div></div></section>}
export function TrustBar(){return <div className="trustbar"><div className="container trustgrid">{[[ShieldCheck,'Reliable Service'],[CheckCircle2,'Quality Work'],[Layers3,'Multiple Services'],[MapPin,'Houston & Surrounding Areas']].map(([I,t]:any)=><div key={t}><I/><strong>{t}</strong></div>)}</div></div>}
export function ServiceCard({s}:{s:Service}){return <article className="card servicecard"><div className="serviceIcon"><Wrench/></div><h3>{s.name}</h3><p>{s.short}</p><Link to={`/services/${s.slug}`}>Learn More →</Link></article>}
export function FAQ({items}:{items:[string,string][]}){return <div className="faq">{items.map(([q,a])=><details key={q}><summary>{q}<ChevronDown/></summary><p>{a}</p></details>)}</div>}
export function Areas(){return <section className="section alt"><div className="container split"><div><div className="eyebrow">Service Area</div><h2>Serving Houston and Surrounding Areas</h2><p>Residential property services throughout Houston and nearby communities.</p><div className="chips">{siteConfig.serviceAreas.map(x=><span key={x}>{x}</span>)}</div></div><div className="mapCard"><MapPin size={48}/><strong>Houston Metro</strong><p>Local service coverage configured for Houston-area projects.</p></div></div></section>}
