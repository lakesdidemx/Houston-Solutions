import { useState } from 'react';
import { Link, NavLink } from 'react-router-dom';
import { Menu, Phone, X } from 'lucide-react';
import { siteConfig, services } from '../data/site';
export function Header(){const [open,setOpen]=useState(false);return <header className="header"><div className="container navrow"><Link className="brand" to="/">Houston <span>Solutions</span></Link><nav className="desktopnav">{siteConfig.nav.map(([n,p])=><NavLink key={p} to={p}>{n}</NavLink>)}</nav><div className="navactions"><a className="btn ghost" href={siteConfig.phoneHref}><Phone size={18}/> Call Now</a><Link className="btn primary" to="/request-estimate">Get a Free Estimate</Link><button className="menuBtn" aria-label="Toggle menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></div>{open&&<div className="mobileMenu">{siteConfig.nav.map(([n,p])=><Link key={p} to={p} onClick={()=>setOpen(false)}>{n}</Link>)}<Link to="/request-estimate" onClick={()=>setOpen(false)}>Get a Free Estimate</Link></div>}</header>}
export function Footer(){return <footer className="footer"><div className="container footergrid"><div><div className="brand light">Houston <span>Solutions</span></div><p>{siteConfig.tagline}</p></div><div><h4>Services</h4>{services.slice(0,9).map(s=><Link key={s.slug} to={`/services/${s.slug}`}>{s.name}</Link>)}</div><div><h4>Company</h4><Link to="/about">About Us</Link><Link to="/projects">Projects</Link><Link to="/service-areas">Service Areas</Link><Link to="/contact">Contact</Link></div><div><h4>Contact</h4><p>{siteConfig.location}</p><a href={siteConfig.phoneHref}>{siteConfig.phone}</a><a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a><p>{siteConfig.hours}</p></div></div><div className="container subfooter"><span>© 2026 Houston Solutions. All Rights Reserved.</span><span><Link to="/privacy-policy">Privacy Policy</Link> · <Link to="/terms">Terms of Service</Link></span></div></footer>}
export function MobileCTA(){return <div className="mobileCta"><a href={siteConfig.phoneHref}>Call Now</a><Link to="/request-estimate">Get Free Estimate</Link></div>}
export default function Layout({children}:{children:React.ReactNode}){return <><Header/><main>{children}</main><Footer/><MobileCTA/></>}
